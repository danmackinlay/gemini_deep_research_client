[project]
name = "gemini-deep-research-client"
version = "0.1.0"
description = "A local client for Google Gemini deep research"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.55.0",
    "gradio>=6.1.0",
]
